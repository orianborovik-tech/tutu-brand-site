/**
 * Asset manifest — the preloader loads exactly this list (30/34/42).
 * REPLACE with the project's real assets:
 *   { name: 'hero', type: 'gltf', path: '/models/hero.glb' }        // from 24, meshopt-compressed
 *   { name: 'env',  type: 'hdr',  path: '/textures/studio-1k.hdr' } // or procedural (23)
 *   { name: 'plate',type: 'texture', path: '/textures/plate.webp' } // background reference (25)
 * The template ships EMPTY so the engine boots with zero network assets
 * (procedural environment + placeholder hero) — proof of life before content.
 */
export default [];
