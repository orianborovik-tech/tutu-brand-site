# 51 — Pre-Flight Checklist (The Final ✓ List)

**Role:** The terminal, mechanical, full-system ✓ list. Nothing ships with an unchecked box. Every box is checked with EVIDENCE (per `04`) and the completed list — with the evidence — becomes SHIP-REPORT.md, delivered to the user. This is the user's contract: בסוף — רשימת וי, בלי טעויות.
**Phase:** 7 (SHIP) — after `50` passes.
**Inputs:** the deployed candidate. **Outputs:** `_process/SHIP-REPORT.md`.

---

## A · Reference fidelity (the user's core promise)

- [ ] Model vs object reference: final side-by-side + IoU > 0.93 + ratios < 3% (`24` numbers pasted).
- [ ] Background vs background reference (case B): side-by-side approved; seven-mismatch pass (`26`).
- [ ] Invented environment (case A/C): archetype named, anti-clone checked, hero owns contrast (`25`).
- [ ] Every detail-inventory item from MODELING-SPEC visible in the shipped model.

## B · Concept & content

- [ ] `50` rubric passed (weighted ≥ 8.5, no axis < 7) — scores pasted.
- [ ] Signature moment present and nameable; H1-story test passes on the LIVE site.
- [ ] Zero placeholder/filler strings (sweep every visible string incl. meta, alt, aria, 404).
- [ ] Hard constraints from BRIEF honored (re-read the user's sentence one last time).

## C · Experience integrity (full journey, cold cache)

- [ ] Loader: real progress, ≤ 2.5s ceremony, one-timeline reveal, revisit skip (`34`).
- [ ] Full scroll pass: 60fps desktop (trace saved), all beats fire both directions, seams clean (`31`).
- [ ] Micro-interactions sweep: zero stock surfaces; hover ≤ 200ms; delight only at low-intent spots (`33`).
- [ ] Page transitions (if any): leak-free ×5 navigation cycle — memory numbers (`35`).
- [ ] Sound (if any): mute-first, toggle persists, no bytes before enable (`36`).

## D · Engineering

- [ ] Budget table 100% green with actual numbers (`41`) — pasted.
- [ ] Tier system + governor verified (forced-tier screenshots incl. designed tier-0 page).
- [ ] Assets: inspect tables, formats, fonts (Hebrew niqqud OK), manifest honest (`42`).
- [ ] Console: ZERO errors/warnings across the full journey (log attached).
- [ ] Memory: baseline → journey → baseline (renderer.info numbers).
- [ ] webglcontextlost handled; visibility-change pauses; resize storm survived (`30`).

## E · Every screen size

- [ ] 320 / 375 / 390 / 768 / 1440 / 1920 screenshots per beat — composed, not cropped (`43`).
- [ ] Real-device matrix run (iOS Safari + mid Android) incl. 3-min thermal session — logged.
- [ ] Landscape-phone state designed; safe-areas respected; no bare 100vh.

## F · Accessibility

- [ ] Reduced-motion: full designed calm version (screenshots) (`44`).
- [ ] Keyboard-only journey + screen-reader pass + contrast over worst-case frames — all logged.
- [ ] Lighthouse A11y ≥ 90 (report saved).

## G · Live & meta

- [ ] Production URL 200/HTTPS; cold-cache filmstrip; cache/MIME headers (`45`).
- [ ] OG card renders (debugger screenshot); favicon; theme-color; title/description in-voice.
- [ ] 404 designed AND returns 404; sitemap/robots/canonical/JSON-LD valid.
- [ ] Shopify (if used): cart round-trip + variant sync on production.
- [ ] Cross-browser: latest Chrome, Safari (macOS + iOS), Firefox, Edge — screenshots.

## H · Honesty gate (`04`)

- [ ] Every box above has evidence attached — none checked from memory.
- [ ] All ASSUMPTIONS.md entries surfaced in SHIP-REPORT (the user sees every decision made for them).
- [ ] Anything skipped is listed as SKIPPED with a reason — nothing silently dropped.
- [ ] WORKLOG shows verification after every phase (spot-audit 5 entries).

---

**Exit:** compile SHIP-REPORT.md = this list ✓'d + evidence + the live URL + assumptions + the
`50` scorecard. Send to the user. THAT message — רשימת הוי עם הראיות — is the deliverable's
closing act, every project, no exceptions.
